# PicDeck 開發規範與 AI 代理指引（Codex / Cursor / 通用規則）

## 語言規範
- 一律使用繁體中文（Traditional Chinese）回答。

## 最高規範準則（Single Source of Truth）
- 專案所有功能佈局、操作手勢與模式行為詳見：`docs/product/PicDeck功能與操作規範.md`。
- 修改任何程式碼前，**必須先查閱該規範文件**，嚴禁破壞既定手勢分層與排版邏輯。
- 若使用者提出新的功能需求或變更既有做法，**必須主動向使用者確認是否同步更新規範手冊**。

## 開發目標設備與安裝指令
每次改動完成後，必須自動安裝並更新至以下三台裝置進行驗證（保留現有相簿資料）：
- iPhone 18 Pro 模擬器: `609C7D8C-52C7-4AED-AB7C-DF5ADE289D0D`
- iPhone 14 Pro Max 模擬器: `A6068813-D220-4272-B019-9DAA57BE0934`
- 實體 iPhone 14 Pro Max: `00008120-0001241C01EB401E`

### 常用指令（於 PicDeck 目錄執行）
- 模擬器建置: `xcodebuild -scheme PicDeck -destination "id=609C7D8C-52C7-4AED-AB7C-DF5ADE289D0D" -derivedDataPath build_sim build`
- 模擬器安裝: `xcrun simctl install <DEVICE_ID> build_sim/Build/Products/Debug-iphonesimulator/PicDeck.app && xcrun simctl launch <DEVICE_ID> com.picdeck.app`
- 實體機建置: `xcodebuild -scheme PicDeck -destination "id=00008120-0001241C01EB401E" -derivedDataPath build_device build`
- 實體機安裝: `xcrun devicectl device install app --device 00008120-0001241C01EB401E build_device/Build/Products/Debug-iphoneos/PicDeck.app`

## 核心不變防禦機制（嚴禁破壞）
1. **單圖檢視（PhotoDetailView）手勢分層**：
   - 資訊面板展開時，面板頂部把手向下滑動**僅收起資訊面板，絕對不可關閉單圖（嚴禁 dismiss 單圖）**。
   - 僅在資訊面板已收合狀態下，照片區域向下滑動超過閥值（> 110pt）才關閉單圖檢視。
2. **中央相片直角呈現**：
   - 相片容器底板維持圓角（RoundedRectangle），中央圖片與影片本體直角呈現（無 clipShape 圓角裁切）。
3. **全域外觀保護**：
   - 嚴禁在任何 Modal、Sheet、單圖檢視中使用 `.preferredColorScheme(.dark)`，避免污染全域 UIWindow 造成淺色模式閃黑或變暗。
4. **照片「全部」檢視生命週期**：
   - 必須使用 ZStack 保留 allGridContent 在 View Hierarchy 中，嚴禁改為 if 銷毀重建，防止黑屏。
5. **備註防抖與雙向同步**：
   - 備註輸入採本地防抖（800ms）及焦點/關閉事件儲存；與系統「照片.app」說明欄位 Caption 雙向同步。
